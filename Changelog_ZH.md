# 更新日志

## v0.2.1

- 优化地图中, 信号检查和提取的性能, 过滤了无关字段, 这个改动能避免涉及到复杂地图时, 注入节点图, 及使用信号发送节点, 在编译时的超长时间等待, 伪卡死问题  感谢 @AncherEven
- 对项目的整个编译, 注入, 资源提取链路, 做了一系列针对的数据复用, 缓存优化, 批量处理, 项目全链路约有50%以上的性能提升
- 项目依赖升级, 注意, 如果你使用旧的模板创建项目, 并且升级了eslint 10, 那么你需要手动安装 @eslint/js, 以使lint生效, 如果你仅升级genshin-ts则不受影响

## v0.2.0

** 当前版本包含潜在的破坏性修改, 更新前请先阅读更新日志 **

- 所有七种客户端节点图现已支持, 用法和服务器节点图相近, 例如直接使用`g.characterSkill().on('start', (_, f) => {})`即可定义角色技能, 你可以悬停相关方法/事件来得到更多信息
- 更新最近版本新增的一系列服务器节点和事件
- 新增和改动一系列eslint规则, 以改善服务器和客户端节点图的开发体验, 注意一些规则的名称有变化, 如果之前你进行了自定义eslint规则设置, 那么名称变化是破坏性的, 需要注意处理
- 更新一系列函数的注释说明, 和额外的gsts note
- 新增了`clientEntity`全局辅助函数, 用于客户端节点图场景, 方便约束实体类型, 并调用那些, 仅在客户端节点图下可用的实体快捷辅助函数
- 节点图注入器改善, 避免了一些注入失败的情况
- 改善关卡全局变量的自动设置, 此前由于千星奇域更新似乎导致了多个同一事件的触发顺序不确定, 即使id=1也不能保证先触发, 对于一些在实体创建时, 取用`stage/level`的情况, 可能导致实体对象无效, 现已通过更好的初始化方式修复
- 修复发送信号, 传入列表类型的参数时, 无效的问题  感谢 @AncherEven
- `querySpecifiedMiniMapMarkerInformation` 查询指定小地图标识信息 节点的返回参数中, 原本的官方文档存在拼写错误, 现已修正, `activationStaet` -> `activationStatus`, 如果你之前使用了此参数, 那么更新后将需要修改, 以避免编译错误
- `getPlayerRankScoreChange`和`setPlayerRankScoreChange`的`settlementStatus结算状态`参数, 枚举类型发生了改变, 并新增了`逃跑`枚举, 如果此前你使用了此方法, 那么更新后, 枚举参数部分需要调整以适配更新
- npm create genshin-ts@latest 模板加入了客户端节点图相关引导
- 其他一些不易察觉的细节问题修复

## v0.1.10

** 当前版本包含潜在的破坏性修改, 更新前请先阅读更新日志 ** - 感谢 @wangxiangyao 和 @longyuan1996 为本次更新做出的一些代码贡献

- 信号发送和监听, 现在支持传参和取用参数, 并且配置过地图注入设置后, 编译时, 会自动提取地图中的所有信号及参数, 并提供完整的ts类型信息, 具体使用可参考 https://gsts.moe/zh/doc/events/signals 文档 #9
- 取节点图变量/自定义变量, 且变量类型是列表时, 现在修改取得的变量, 将会直接影响到原始值, 而以前的版本, 修改的是列表的拷贝, 现在你需要手动f.copyList来得到一个列表拷贝; 旧的逻辑中, 想要直接修改原始列表值很不方便, 并且在同样形式的代码中, 可能产出不一样的结果, 不符合直觉, 因此这被视作bug并修复, 现在可以产出更简洁高效的节点图, 具体可参考 https://gsts.moe/zh/doc/writing/ts-subset 文档 #11
- 更新千星奇域新版本增加的节点, 此外 `consumeGiftBox` 和 `getEntityAdvancedAttribute` 的返回值有变化, 这可能潜在导致升级后编译出错, 需要注意, 各函数节点的描述也已与官方文档同步
- 现在switch case值为常量时, 也能够正常编译 #12
- 常量表达式取用时, 现在更少产生冗余的局部变量节点 #12
- 预设资源新增所有角色id: `CharacterPrefabZh` 和 `CharacterPrefab`
- 修复当一个变量被赋值为字面量常量时, 即使声明了let, 也没有正常使用局部变量节点的问题
- 修复在一个循环中, 对循环变量取模时, 错误地处理为float的问题, 千星奇域中取模仅支持整形, 现在强制处理为整形
- 编译中间产物json新增了一系列节点别名支持, 因为官方对一些节点的英文名做了修改, 现在新旧名称都能通过编译
- create-genshin-ts 项目模板更新

## v0.1.9

- 更新最近版本新增的一系列服务器节点和事件
- 更新旧节点, 一些节点在新版本中有新增参数, 现在已经同步到最新版
- 将节点函数的注释描述和官方文档同步, 官方文档有更新的节点使用说明

## v0.1.8

- 项目模板新增额外的编辑器与代码职责划分说明, 以便引导ai开发

## v0.1.7

- 修复先前版本中, gia编码缺少对 元件ID 列表, 用作字典值 并传参时的支持, 导致编译生成的节点图, 相关节点参数没有正常连线问题

顺便一提: 上月我忙于一些现实中的事物, 因此搁置了项目的开发, 接下来将继续开发

## v0.1.6

- 针对数组索引取值的情况, 由于本项目使用了bigint表示整型, 而typescript默认不允许bigint索引取值, 针对这一问题, 该版本引入了eslint和typescript插件两种自动修复手段, 你可以直接通过eslint得到详情介绍
- npm create genshin-ts@latest的项目模板有不小幅度的更新
- 修复先前版本类型系统改动, 导致的中文事件悬停提示的注释失效

## v0.1.5

- 允许编写js常见风格的setInterval自清理回调, 例如
    ```ts
    const h = setInterval(() => {
        f.printString('hello')
        if (condition) {
            clearInterval(h)
        }
    }, 1000)
    ```
- 允许在定时器回调内修改外部let声明的变量, 包括数组push等修改, 使其效果符合js语法直觉, 以方便许多常见用例, 先前版本会编译出错. 现在像这样的代码是可以正常编译的
    ```ts
    let acc = 5n
    const h = setInterval(() => {
        acc += 2n
        f.printString(str(acc))
        if (acc >= 9n) {
            clearInterval(h)
        }
    }, 1000)
    ```
- 修复先前版本中, 在定时器回调内, 使用嵌套回调导致变量没有正确捕获的问题, 例如
    ```ts
    setTimeout(() => {
        nums.forEach((v) => {
            f.printString(label)
            f.printString(str(v + base))
        })
    }, 200)
    ```
  情况下, 仅nums被捕获, 而label, base没有被正确捕获的问题
- 修复在定时器回调中, 调用函数时, 函数被错误地当作变量捕获, 引发的编译错误
- 数组foreach的回调函数参数, 不再强制要求, 允许为空

## v0.1.4

- 修复上个版本因为实体类型变得更加复杂, 导致的一些情况下eslint和定时器闭包捕获, 对实体类型显示错误的情况; 现在编译器的实体类型推断使用更智能的方式避免因为复杂的类型系统出错
- dev模式下, 外部编辑器保存地图时, 现在会自动重新注入数据, 意外覆盖时, 不再需要手动重新注入, 可以在配置中调整此设置
- 新增支持自动从地图中提取自定义元件的名称和id到代码, 方便定位操作对象和交互, 你可以在配置中调整开关和提取路径
- 添加更严格的超限模式和经典模式节点校验, 避免在使用快捷方法的情况下, 意外引入不可用的节点, 现在这种情况会直接提示错误
- eslint新增检查, 在定时器回调中, 直接访问外层事件参数的情况, 现在会显示警告, 引导进行修正

## v0.1.3

- 类型系统现在自动约束超限模式和经典模式可用的节点
- 支持dict(0), list(0), 等占位方法, 以便处理一些情况下, 节点参数引脚需要留空
- 为raw/float/int/guid/list/dict等辅助函数补充更详细的悬停注释说明, 和使用场景说明, 以便明确用途
- 新增一个经典模式下的节点`复苏当前场上角色`
- 修复`传送玩家`节点在经典模式下的创建异常

## v0.1.2

- g.server()支持传入mode字段, 控制超限模式和经典模式
- 新增6.3版本更新加入的14个新的服务器节点, 并添加相关的实体辅助属性和方法, 如.activeCharacter, .classicModeId等
- 类型系统目前尚未区分超限模式和经典模式可用的节点, 所有节点不分模式, 均可注入, 目前需要人为注意相关节点的使用

## v0.1.1

- 首次发布
